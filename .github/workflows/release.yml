name: Release

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container: archlinux/archlinux:base-devel
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go/build
          key: go-${{ hashFiles('go.sum') }}
          restore-keys: go-

      - name: Update system and install makedeps
        run: |
          pacman -Syu --noconfirm
          pacman -S --noconfirm --needed go npm imagemagick webkit2gtk-4.1 github-cli git openssh

      - name: Run tests
        run: go test ./internal/... ./pkg/...

      - name: Build binary
        run: |
          cd frontend && npm ci && VITE_API=wails npm run build && cd ..
          go build -tags webkit2_41,production -ldflags "-s -w" -o anus .
          chmod -R u+w "$(go env GOMODCACHE)" 2>/dev/null || true

      - name: Bundle release tarball
        if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
        run: |
          PKGVER=${GITHUB_REF_NAME#v}
          DIST="anus-${PKGVER}"
          mkdir -p "${DIST}/icons"
          cp anus "${DIST}/"
          cp anus.desktop "${DIST}/"
          cp LICENSE "${DIST}/"
          cp assets/appicon.svg "${DIST}/anus.svg"
          for sz in 16 32 48 64 128 256; do
            mkdir -p "${DIST}/icons/${sz}x${sz}"
            magick -background none assets/appicon.svg -resize "${sz}x${sz}" "${DIST}/icons/${sz}x${sz}/anus.png"
          done
          tar czf "${DIST}-linux-x86_64.tar.gz" "${DIST}"
          echo "TARBALL=${DIST}-linux-x86_64.tar.gz" >> "$GITHUB_ENV"
          echo "PKGVER=${PKGVER}" >> "$GITHUB_ENV"

      - name: Create or update release
        if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: |
          gh release view "v${PKGVER}" > /dev/null 2>&1 || \
            gh release create "v${PKGVER}" --title "v${PKGVER}" --generate-notes --latest
          gh release upload "v${PKGVER}" "${TARBALL}" --clobber

      - name: Update PKGBUILD sha256 on main
        if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          SHA256=$(sha256sum "${TARBALL}" | cut -d' ' -f1)
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git clone "https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}" /tmp/repo
          cd /tmp/repo
          sed -i "s/^pkgver=.*/pkgver=${PKGVER}/" packaging/PKGBUILD
          sed -i "s/^sha256sums=.*/sha256sums=('${SHA256}')/" packaging/PKGBUILD
          sed -i "s/\"productVersion\": \".*\"/\"productVersion\": \"${PKGVER}\"/" wails.json
          git add packaging/PKGBUILD wails.json
          git diff --staged --quiet || git commit -m "Release v${PKGVER}: update PKGBUILD sha256 and wails.json version"
          git push origin main

      - name: Push to AUR
        if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
        env:
          AUR_SSH_KEY: ${{ secrets.AUR_SSH_KEY }}
        run: |
          mkdir -p /root/.ssh
          echo "$AUR_SSH_KEY" | tr -d '\r' > /root/.ssh/aur
          chmod 600 /root/.ssh/aur
          export GIT_SSH_COMMAND="ssh -i /root/.ssh/aur -o StrictHostKeyChecking=accept-new"
          git config --global --add safe.directory '*'
          git clone ssh://aur@aur.archlinux.org/anus.git /tmp/aur-anus
          cp /tmp/repo/packaging/PKGBUILD /tmp/aur-anus/PKGBUILD
          useradd -m aurbuild
          chown -R aurbuild /tmp/aur-anus
          su -c 'cd /tmp/aur-anus && makepkg --printsrcinfo' aurbuild > /tmp/aur-anus/.SRCINFO
          cd /tmp/aur-anus
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add PKGBUILD .SRCINFO
          git diff --staged --quiet || git commit -m "Release v${PKGVER}"
          git push

      - name: Push anus-git to AUR
        if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
        env:
          AUR_SSH_KEY: ${{ secrets.AUR_SSH_KEY }}
        run: |
          export GIT_SSH_COMMAND="ssh -i /root/.ssh/aur -o StrictHostKeyChecking=accept-new"
          git clone ssh://aur@aur.archlinux.org/anus-git.git /tmp/aur-anus-git
          cp /tmp/repo/packaging/PKGBUILD-git /tmp/aur-anus-git/PKGBUILD
          chown -R aurbuild /tmp/aur-anus-git
          su -c 'cd /tmp/aur-anus-git && makepkg --printsrcinfo' aurbuild > /tmp/aur-anus-git/.SRCINFO
          cd /tmp/aur-anus-git
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add PKGBUILD .SRCINFO
          git diff --staged --quiet || git commit -m "Release v${PKGVER}"
          git push

      - name: Upload artifact
        if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v7
        with:
          name: anus-linux-x86_64
          path: "*.tar.gz"
          if-no-files-found: error
